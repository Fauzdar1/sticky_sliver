name: sticky_sliver
description: Sample Sticky Sliver project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.3 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2 #https://pub.dev/packages/cupertino_icons
  fluttertoast: ^8.0.9 #https://pub.dev/packages/fluttertoast

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0 #https://pub.dev/packages/flutter_lints


flutter:

  uses-material-design: true
